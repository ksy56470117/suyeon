## 오늘 할 일
- [ ] 토스프라임초대 김예진01043481929
- [ ] 토스프라임 초대 설은향01088610417
- [ ] 종묘와 사직단 조성의 역사 2 %%tag:한국의문화유산역사여행%%
- [ ] 종묘와 사직단 조성의 역사 1 %%tag:한국의문화유산역사여행%%

```dataview
TASK FROM "Recurring Tasks"
WHERE !completed
WHERE choice(
  contains(text, "🔁 every day"), true,
  choice(
    contains(text, "🔁 every weekday") AND (dateformat(date(today), "E") != "6") AND (dateformat(date(today), "E") != "7"), true,
    choice(
      contains(text, "🔁 every monday") AND dateformat(date(today), "E") = "1", true,
      choice(
        contains(text, "🔁 every tuesday") AND dateformat(date(today), "E") = "2", true,
        choice(
          contains(text, "🔁 every wednesday") AND dateformat(date(today), "E") = "3", true,
          choice(
            contains(text, "🔁 every thursday") AND dateformat(date(today), "E") = "4", true,
            choice(
              contains(text, "🔁 every friday") AND dateformat(date(today), "E") = "5", true,
              false
            )
          )
        )
      )
    )
  )
)
```

- [ ]

---

## 공부 기록

| 과목  | 시작  | 종료  | 시간  |
| --- | --- | --- | --- |
|     |     |     |     |

---

## 메모

